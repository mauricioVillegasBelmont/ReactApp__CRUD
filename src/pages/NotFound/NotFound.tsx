import React from "react";
import styles from "./NotFound.module.scss";



const NotFound: React.FC = () => {
  return (
    <>
      <p className={`${styles.NotFound}`}>page - NotFound</p>
    </>
  );
};

export default NotFound;