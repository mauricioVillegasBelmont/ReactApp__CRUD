import React from "react";
import styles from "./Cart.module.scss";


const Cart: React.FC = () => {
  return (
    <>
      <p className={`${styles.Cart}`}>page - Cart</p>
    </>
  );
};

export default Cart;